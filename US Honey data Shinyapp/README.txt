init_statedata perfoms modifications on honeydata.csv and outputs a csv.file called states.dat that contains the geographic delimitations of each US state along with Honey production statistics per state.
ui generates the desired user interface of our shiny app.
server hosts the server, loads the states.dat csv file and draws all graphs
